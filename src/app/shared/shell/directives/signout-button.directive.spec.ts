import { SignoutButtonDirective } from './signout-button.directive';

describe('SignoutButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SignoutButtonDirective();
    expect(directive).toBeTruthy();
  });
});
